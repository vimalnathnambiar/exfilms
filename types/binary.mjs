/**
 * @typedef param
 * @property {base64} base64 Base64 configuration
 * @property {string | null} array Array type
 */

/**
 * @typedef base64
 * @property {[number, string] | null} type Data type
 * @property {string | null} compression Compression type
 */
